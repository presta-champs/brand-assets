# Brand assets

Images referenced by the PrestaChamps / PayPerChamps email signatures.

These are hotlinked directly from signature HTML, so **do not rename, move or
delete these files** — doing so breaks the photo in every signature already
sitting in someone's mail client.

| File | Used by |
|---|---|
| `tamas-avatar.png` | Tamás Nagy — PrestaChamps (EN/RO), PayPerChamps (EN/RO) |
| `leo-avatar.png` | Leo Jozsa — PrestaChamps (EN/RO) |

Both are 240×240 PNG, sized for display at 100×100 in a circular crop.
